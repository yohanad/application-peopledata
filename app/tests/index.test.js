test('serious test', () => {
    expect(true).toBe(true);
});

test('more serious test', () => {
    expect(false).toBe(false);
});

test('real serious test', () => {
    expect(1).toBe(1);
});

test('extreme serious test', () => {
    expect("a").toBe("a");
});